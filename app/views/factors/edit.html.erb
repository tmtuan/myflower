<div class="container">
  <h1>Edit factor</h1>
  <%= form_for :factor, url: factor_path(@factor), html: { :class => "form-group" }, method: :patch do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, :class => "form-control" %>
      </div>

      <%= f.submit "Save", :class => "btn btn-primary btn-lg" %>

  <% end %>
</div>